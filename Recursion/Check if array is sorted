class Solution {
  public:
    bool check(vector<int>& arr, int i, int n){
        if(i==n || i==n-1) return true;
        if(arr[i]>arr[i+1]) return false;
        return check(arr,i+1,n);
    }
    bool isSorted(vector<int>& arr) {
        return check(arr,0,arr.size());
    }
};
